import React, { FC } from 'react';
import { NavBar } from './components';

const Header: FC = () => {
    return (
        <div className="Header" >
            <NavBar />
        </div >
    );
}

export { Header };
